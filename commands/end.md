# /end

Follow `AGENTS.md` as the source of truth.

Before stopping or switching agents:

1. Compact `agent-memory/STATE.md`.
2. Rewrite `agent-memory/HANDOFF.md`.
3. Append `agent-memory/WORKLOG.md` only if the repo changed meaningfully.

Do not update `agent-memory/PROJECT.md` unless stable verified facts changed.
Do not update `agent-memory/DECISIONS.md` unless a durable decision was made.

Final response must include:

- Summary
- Files changed
- Verification
- Current status
- Next recommended step
- Memory updated
